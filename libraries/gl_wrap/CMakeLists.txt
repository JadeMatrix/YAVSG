ADD_LIBRARY( gl_wrap INTERFACE )
TARGET_INCLUDE_DIRECTORIES( gl_wrap INTERFACE "include/" )

FIND_PACKAGE( GLEW REQUIRED COMPONENTS GLEW )
FIND_PACKAGE( OpenGL REQUIRED )
TARGET_LINK_LIBRARIES( gl_wrap INTERFACE GLEW::GLEW OpenGL::GL )
