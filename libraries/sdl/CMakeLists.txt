ADD_LIBRARY( sdl )
TARGET_SOURCES( sdl PRIVATE "src/utils.cpp" )
TARGET_INCLUDE_DIRECTORIES( sdl PUBLIC "include/" )

FIND_PACKAGE( SDL2 2.0.8 REQUIRED COMPONENTS sdl )
TARGET_LINK_LIBRARIES( sdl PUBLIC SDL2::sdl gl_wrap )

TARGET_COMPILE_OPTIONS( sdl PRIVATE ${YAVSG_COMPILE_OPTIONS} )
